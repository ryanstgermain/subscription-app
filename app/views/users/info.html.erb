<div class="container">
    <h3>Account for <%= current_user.email %></h3>
    <% if @subscription.active %>
        <h3>Current plan renews at: <%= Time.at(@stripe_subscription["current_period_end"]).strftime("%D") %></h3>
    <% else %>
        <%= render 'form' %>
    <% end %>
</div>
